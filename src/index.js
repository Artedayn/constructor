import {model} from './model'
import { App } from './classes/app'
import './style/main.css'


new App(model)


//const key = 'title'
//console.log(templates[key])





//model.forEach(element => {
    //let html = ''

    // if(element.type === 'title') {
    //     html = title(element)
    // }
    // else if(element.type === 'text') {
    //     html = text(element)
    // }
    // else if(element.type === 'columns') {
    //     html = columns(element)
    // }
    // else if(element.type === 'image'){
    //     html = image(element)
    // }

    //const toHTML = templates[element.type]
    //console.log(element.toHTML);
    
    //site.insertAdjacentHTML('beforeend', element.toHTML())    
//})


